# macOS does not have the KDE-style modifier+drag window move behavior, so keep
# native window decorations enabled there to preserve a draggable titlebar.
hide_window_decorations no
macos_titlebar_color background
